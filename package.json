{
  "name": "mepco-help-desk",
  "version": "0.1.0",
  "private": true,
  "description": "MEPCO Integrated Help Desk & Ticket Management System",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "install:all": "npm install --prefix backend && npm install --prefix frontend",
    "dev:backend": "npm run dev --prefix backend",
    "dev:frontend": "npm run dev --prefix frontend",
    "db:migrate": "npm run db:migrate --prefix backend",
    "db:status": "npm run db:status --prefix backend",
    "db:seed": "npm run db:seed --prefix backend",
    "db:bootstrap-admin": "npm run db:bootstrap-admin --prefix backend",
    "db:purge-sample-data": "npm run db:purge-sample-data --prefix backend",
    "db:reset": "npm run db:reset --prefix backend",
    "db:setup": "npm run db:setup --prefix backend",
    "db:backup": "npm --prefix backend run db:backup --",
    "db:restore": "npm --prefix backend run db:restore --",
    "build": "npm run build --prefix backend && npm run build --prefix frontend",
    "typecheck": "npm run typecheck --prefix backend && npm run typecheck --prefix frontend",
    "lint": "npm run lint --prefix backend && npm run lint --prefix frontend",
    "test": "npm run test --prefix backend && npm run test --prefix frontend",
    "verify": "npm run lint && npm run typecheck && npm run test && npm run build"
  }
}
